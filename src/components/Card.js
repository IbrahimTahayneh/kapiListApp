import React from "react";
import { View, TouchableOpacity } from "react-native";
const Card = () => {
  return (
    <TouchableOpacity>
      <Text>Hello</Text>
    </TouchableOpacity>
  );
};

export default Card;
